# Node
node_modules/

# Local settings that could contain a token or a real budget id.
# The app itself keeps nothing on disk, but be certain.
*.local.json

# Editors and OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Claude Code local tooling config (per-machine, not part of the app)
.claude/
